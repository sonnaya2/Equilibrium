import { Stub } from "@/components/Stub";

export default function CombatPage() {
  return (
    <Stub
      title="Combat"
      note="Quick / Build / Analysis / Rotation. Engine core lives in src/combat and is React-free; only damagePerLevel is implemented so far."
    />
  );
}
