import type { ReactNode } from "react";
import "@/concepts/build-showcase/build-showcase.css";

export default function BuildShowcaseLayout({ children }: { children: ReactNode }) {
  return children;
}
