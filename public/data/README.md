# Generated frontend data

Everything below this directory is generated by `npm run data:rebuild` and ignored by Git. Do not
edit shards or manifests by hand, and do not read them in bulk to answer a question about content —
use `npm run data:find`, `data:show` or `data:context` against the database instead, and regenerate
with `data:export:changed`.
