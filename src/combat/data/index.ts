// Typed accessors over the canonical JSON store at repo-root `data/combat/`.
// The sync scripts own those files; nothing here should hand-edit them.
// TODO: loaders + record types for abilities, equipment, prayers, perks, effects, update-index.
export {};
