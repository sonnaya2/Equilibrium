// ponytail: poison bypasses hit chance (changelog, 4 Mar 2024) and weapon poison+++ /
// kwuarm incense are sourced as existing, but no current poison damage numbers are in the
// corpus. Upgrade trigger: sourced per-tier poison values, then model as postHit damage that
// skips the Damage Potential stage.
export {};
