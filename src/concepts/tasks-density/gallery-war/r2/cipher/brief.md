# Cipher Strip · Gallery War R2

**Codename:** Cipher Strip  
**Id:** `cipher`  
**Preview:** `CipherPreview.tsx` · skin `.td-gw-cipher` in `cipher.css`  
**Scope:** concepts lab only — no production `/tasks` edits.  
**R1 score:** 8.95 (scan leader) · **R2 target:** ≥9.2 composite

---

## Thesis (R2)

**Mono Comp%/pts ribbon remains the scan instrument.** R2 marries it to Crucible full-set reach and Sigil detail width, under Grove-pressure column density.

```
┌─ FACETS: Cipher · count · virt n/total · search · build · region · tiers ─┐
│  tile  tile  tile  tile     ← denser minmax ~12.75rem, solid rest         │
│  ┌─ ribbon ────────────┐                                                  │
│  │ easy · Glob  42% 10 │  ← mono left-loc / right-figures (kept)         │
│  └─────────────────────┘                                                  │
│  ━━━━━ focus band under selected row (full width, long desc) ━━━━━       │
│  every filtered task reachable via row virt — no slice(0,120)             │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## Mandatory surgery applied

| Must | R2 move |
|---|---|
| Full-set ops | `useVirtualizer` over rows + ResizeObserver cols; `virt n/total` in bar; no 120 cap |
| Detail width | Sigil-style focus band under selected row (`measureElement` on expand); no in-tile postage drawer |
| Density | minmax pressure ~12.75rem (`MIN_CARD_PX`); tight gaps; 15px names retained |
| Foot | Cue only (`Details`/`Open`) — region lives on ribbon, never restated in foot |
| Idle chrome | Solid `stone-800` tiles at rest; gem border/inset only on select |

**Keep (R1 wins):** mono ribbon columns, left-loc / right-figures, quiet head, 15px names, checkbox ≠ expand.

**Stolen axes:** Crucible reach · Sigil band · Grove columns · Ash foot discipline · Oracle rest solid (no multi-stop wash).

---

## Fixed recipe

- `useTasksDesk` + real Catalyst props — no invented rows
- Gallery topology; expand = full-width band under row (not side inspector)
- Checkbox `stopPropagation` — complete ≠ expand; `aria-controls` → band
- Editorial Echo tokens under `.td-gw-cipher` only
- Crystal facet chips; gem interactive / gold display
- Comp% wiki deep-links; My build / region / tier / search / progress
- Humanizer: no marketing copy, no glass garden, no gold CTAs

---

## Signature bet

The **cipher ribbon** is still the product read in &lt;3s. Virt + band are infrastructure that remove R1 ops/detail debts so the ribbon can compete for 9.2 instead of plateauing at 8.95.

---

## Self-score (author — honest, not craft cosplay)

| Axis | Wt | R1 | R2 | Note |
|---|---|---:|---:|---|
| Scan / readability | 25 | 9.1 | **9.2** | Ribbon kept; denser grid still columns Comp%/pts; names 15px |
| Viewport fill | 20 | 8.8 | **9.1** | Shell −6rem; Grove-pressure cols; band costs one row only when open |
| Operability | 20 | 8.9 | **9.4** | Full filtered set via virt; measureElement; filters + wiki + progress |
| Human craft | 15 | 8.7 | **8.9** | Band width for long desc; solid rest; restrained select |
| Anti-slop | 10 | 9.1 | **9.2** | No idle multi-stop wash; gem for state; no foot redundancy |
| Signature | 10 | 9.2 | **9.2** | Ribbon still the instrument; virt/band are peer infrastructure |
| **Weighted** | | 8.95 | **~9.18** | |

Pass bar **9.2** — **honest high-9 / borderline clear.** Ops + density + detail width move the composite; scan/signature held. Risk: CEO may still ding band height cost vs closed-column count, or want tighter chrome (Quill track) on top. Not self-scored as a free 9.5.
